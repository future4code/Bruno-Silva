import { indexOf } from "./indexOf";

const source: string = "bananinha";
const query: string = "ni";

console.log(`source: '${source}'`);
console.log(`query: '${query}'`);
console.log(`indexOf: ${indexOf(source, query)}`);