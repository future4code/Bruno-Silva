import styled from 'styled-components';
import Card from '@material-ui/core/Card';

export const CreateCommentBox = styled(Card)`
    width: 320px;
    margin: 24px;
    padding: 16px 0;
    
    form {
        margin-top: 24px;
        margin-bottom: -16px;
    }
`