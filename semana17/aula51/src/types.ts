export type userAddress = {
   zipcode: string,
   street: string,
   numberAdd: number,
   complement?: string,
   neighbourhood: string,
   city: string,
   state: string
};

export type addressInfo = {
   street: string,
   neighbourhood: string,
   city: string,
   state: string
};